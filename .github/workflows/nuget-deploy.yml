# -----------------------------------------------------------------------------
# GitHub Actions Workflow: Publish to NuGet
#
# Purpose:
#   This workflow builds, tests, and publishes the Numeri Romani .NET library
#   to NuGet when a new release is published in the main branch.
#
# Prerequisites:
#   - The repository must contain a valid .NET project.
#   - The NuGet API key must be stored in the repository secrets as NUGET_API_KEY.
#   - The release tag must follow the format: v<major>.<minor>.<patch> (e.g., v1.2.3).
# -----------------------------------------------------------------------------

name: Publish to NuGet

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      DOTNET_VERSION: '10.0.x'

    permissions:
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Run tests
        run: dotnet test --configuration Release --no-build --verbosity normal

      - name: Extract version from release
        id: get_version
        run: |
          VERSION=$(echo "${{ github.event.release.tag_name }}" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Validate version
        run: |
          if [ -z "${{ env.VERSION }}" ]; then
            echo "Error: Failed to extract version from release tag. Ensure the tag follows the format v<major>.<minor>.<patch>."
            exit 1
          fi

      - name: Pack
        run: dotnet pack --configuration Release --no-restore --output ./nupkg /p:Version=${{ env.VERSION }}

      - name: Publish to NuGet
        run: dotnet nuget push ./nupkg/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json
